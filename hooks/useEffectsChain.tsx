"use client";

import type { EffectsChain } from "@/utils/effectsUtils";

/**
 * Stub hook: no effects chain; always returns null.
 * @returns null (no effects chain)
 */
const useEffectsChain = (): EffectsChain | null => null;

export default useEffectsChain;
